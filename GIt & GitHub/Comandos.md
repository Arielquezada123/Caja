
---


````bash
git config --global user.name "Ariel"

git config --global user.email "ariel.quezada06@inacapmail.cl"

git init

git status

git log

git add "nombrearchivo"(o un . para agregar todos)

git commit -m "comentario"

git branch -m "nombrerama"

git reset

git ls-tree --name-only  -r HEAD  (para ver que archivos estan en la rama)
git ls-tree --name-only  -d HEAD (para ver las carpetas que de cabeza en la rama)

git checkout  "GIt & GitHub/Inicio Git.md"
```
